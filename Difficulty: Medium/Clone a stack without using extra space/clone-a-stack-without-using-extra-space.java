// User function Template for Java

class Solution {
    void clonestack(Stack<Integer> st, Stack<Integer> cloned) {
      for(int num:st){
          cloned.push(num);
      }
      //cloned.reverse();
    }
}